# Railway npm config
# Use legacy-peer-deps to avoid peer dependency resolution failures
legacy-peer-deps=true
# Don't run audit on install — saves build time
audit=false
# Don't run fund messages
fund=false
